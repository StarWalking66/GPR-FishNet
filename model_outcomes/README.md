# Model Outcomes

This directory is reserved for generated experiment outputs, checkpoints, predictions, metrics, and figures.

Large generated files are not tracked in Git. Pretrained checkpoints or full experiment outputs should be shared through an external archive or release link when needed.

